@page
@model BookedWebApp.Pages.HotelsModel
@{
	Layout = "_HomeLayout";
}

<div class="container">
    <h1>All hotels</h1>
    <div class="row">
        @foreach (var hotel in Model.DetailsHotel)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100">
                    <img class="card-img-top" src="@($"data:image/png;base64,{Convert.ToBase64String(hotel.Image)}")" alt="Card image cap">
                    <div class="card-body">
                        <h5 class="card-title">@hotel.Name</h5>
                        <p class="card-text">Price per night: @hotel.PricePerNight</p>
                        <a class="btn btn-primary" asp-page="/HotelDetails" asp-route-id="@hotel.HotelId">More details</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<div class="container">
    <h1>All hotels</h1>
        @foreach (var hotel in Model.DetailsHotel)
        {
            <div class="card mb-3" style="max-width: 755px;">
              <div class="row g-0">
                <div class="col-md-4">
                    <img class="card-img-top" src="@($"data:image/png;base64,{Convert.ToBase64String(hotel.Image)}")" alt="...">
                </div>
                <div class="col-md-8">
                  <div class="card-body">
                    <h5 class="card-title">@hotel.Name</h5>
                    <p class="card-text">Price per night: @hotel.PricePerNight</p>
                    <a class="btn btn-primary" asp-page="/HotelDetails" asp-route-id="@hotel.HotelId">More details</a>
                  </div>
                </div>
              </div>
            </div>
        }
</div>

